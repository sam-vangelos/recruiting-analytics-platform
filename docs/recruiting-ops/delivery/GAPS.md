# Recruiting-ops delivery gaps

Updated 2026-07-20 for the durable copy-only rollout. These are
platform or governance inputs; the adapter must not compute or improvise them.

| Gap / field | Required grain | Consumer | Classification / next owner |
|---|---|---|---|
| Reviewed release provenance and dark-launch transport | Merged SHA × immutable image digest × private service/Job execution | Unified copied-artifact Scheduler | **Partially closed on 2026-07-20.** PR #6 merged as `a5a0ea1a56e47599ef6940ace9aedcc8323b20ec`; strict required `verify` CI and the clean Cloud Build passed. Private service generation 97 and dark Job generation 16 use the same digest `sha256:06138fdb9a9af7650492af9f18430c8304f47971260b722d441d06b8d07f1624`, matching source labels, unchanged IAM, and unchanged non-image templates. Monday 06:30 dry execution `ta-ops-staging-hydration-h2fzc` resolved only `all_hires`, completed `no_change`, made zero mutations, and preserved copy/canonical Drive 23/486. Invalid 06:31 execution `ta-ops-staging-hydration-h29k6` failed before Greenhouse, Google, or durable-run construction; Cloud Monitoring recorded the expected failed-execution delta. Remaining dark gates are Monday 23:30 dry proof, one real non-Thursday Scheduler fire, and inbox confirmation for the Job-failure and Scheduler-change alerts. None of this advances an artifact's five-shadow count. |
| Stable candidate-event key plus `application_id`, canonical stage, event type, event timestamp, current status, rejection/withdrawal direction | Reporting week × requisition × application × stage event | Four pipeline copies; Weekly Progress candidate view | **Closed for the copy orchestrator.** The immutable source cut now persists normalized candidate events with stable application/stage identity and the Final Offer direction fields. E01 remains aggregate-only; consumers outside this orchestrated copy path still need an equivalent platform contract. |
| True origin-stage attribution from stage-history `exited_at` | Application stage-exit event | Pipeline and ELT passed attribution | Platform refinement; do not shift stages adapter-side. |
| Weekly Progress stage-row measure (`conducted`, `entered`, or `passed`) | Report tab × reporting week × canonical stage | Weekly Progress summary tabs | **Closed by explicit approval on 2026-07-17.** The copy contract uses passed-stage application counts, scorecard-grain RPS Conducted, and offer-lifecycle counts. This semantic approval governs copy maintenance; it does not adjudicate the 2-of-6 FDE/PE canonical differences or authorize canonical promotion. |
| Recurring weekly value-only ranges and comparable current-week baselines for three Weekly Progress role tabs | Reporting week × role tab × stage | Weekly Progress | **Closed for copy maintenance; canonical comparison and shadows remain.** The planner inserts the missing weekly destination before the live `QTD` column, preserves prior columns, applies the approved hybrid semantic, and treats the complete after-state as a no-op. Code/RL and Brazil/Colombia still lack comparable canonical weekly baselines, and five real Thursday shadows remain. |
| Observed-state candidate-tab creation and in-filter append ranges | Reporting week × pipeline workbook | Four pipeline copies | **Closed for Step 4; weekday shadows remain.** Each pipeline produced the same 4-forward/2-rollback zero-mutation plan twice, then completed one guarded two-call structural/value canary. Independent readback found exactly one reserved target, exact after-structure, a zero-difference writer-source plan, and unchanged predecessor/history (719, 632, 1,175, and 1,043 retained job rows respectively). Terminal replays added no attempts or Google calls. Pipeline 890 also completed its first real weekday shadow at zero mutations and Drive 41→41; the other three still require all five. The Job template is dark, the durable stop is engaged, and Tier 2 is not enabled. |
| Dynamic pivot source including appended hire rows | Hire row / stable offer or application key | All Hires visible pivot | Manual demonstration exception. Tier 1 safely extended the value layer to 39 data rows and proved a settled no-op without changing topology. The Jul 17 scheduled-time value-layer shadow independently reconciled all 39 rows and 11 governed hires, made zero mutations at Drive 23→23, and counts as All Hires observation 1 of 5; it did not touch or waive the pivot gap. The automated copy pivot mutation remains quarantined because Google's earlier forward/rollback cycle did not reproduce the exact serialized preimage. the operator will reset/retrigger the copy pivot manually, followed by read-only verification; this cannot be claimed as fully automated parity. |
| Canonical/platform adjudication for Weekly Recruitment | Five governed value surfaces × immutable source cut | Weekly Recruitment | The 2026-07-12 private-GCP prewrite audit found 444 differences across all five surfaces. A read-only follow-up proved the untouched copy and canonical were identical across the same 3,420 governed cells before hydration, isolating the issue to live projection versus the frozen manual baseline. The latest copy audit at Drive 48 found all five source-bound desired ranges exact, while the canonical remained 23408 with no comparable Jul 10–16 tab. That proves copy state, not which differences are platform-correct, legacy error, or source drift. Retain only fingerprint-bound parity approvals. |
| Weekly Recruitment rollover and missing-role row lifecycle | Reporting Friday × exact predecessor × missing team-visible req | Weekly Recruitment | **Implemented and Tier 1 copy-proven.** The service created `Weekly Working Report Sheet 10 Jul to 16 Jul 2026` exactly once. The reversible whole-row lifecycle added missing team-visible reqs, repaired inherited form, and moved a newly non-open role below the unique `Closed Jobs` divider without detaching `D/G/J:K/L/X`. A fresh durable no-op held Drive 48→48; independent readback found the same 176 rows, unique divider at row 38, and all 138 non-open roles below it. Observed-predecessor, exact-divider, full-row rollback, no-op, and Dec→Jan behavior are retained in tests. Five real weekday shadows remain. |
| Daily Delivery Roles RPS report contract | Pacific business date × recruiter-screen submission × team / submitter / match / role / raw sections | Delivery Roles RPS | **Closed for the supervised copy; recurrence and promotion remain gated.** The prior Thursday-only team summary was incomplete and is superseded. Commits `eb867a0`/`8216fa1` render the complete canonical-shaped `A3:N` report for each date and derive the reporting week from that Pacific business date across UTC midnight. Source cut `cdc6b40a-f986-4be9-bf41-22e7ae6954ef` populated Jul 13–16 with 20/16/19/8 rows; all five sections reconcile, exact formatting matches, and all four source replays are no-ops. Canonical Drive 705 was unchanged. The weekday 23:30 Pacific copy-only Scheduler remains paused and dry-run-only until five real shadows; canonical mutation still requires an explicit promotion approval. |
| Value-coupled Google hyperlink certification | Changed label or URL cell × typed value × full-cell link URI | Weekly Recruitment and future URL writers | **Implemented and Tier 1 accepted.** The earlier Jul 10 batch proved Google's URL auto-link materialization. Tier 1 exposed one changed linked label whose URI matched an unchanged exact string URL in the same physical row. The narrow replacement preserves only that stable companion link, still validates changed URL auto-links, and rejects absent, formula-derived, changing, stale, or out-of-range companions. The guarded mutation reached Drive 48 with exact desired values and retained link topology; the fresh run durably certified `no_change`, zero mutations, and Drive 48→48. |
| ELT output PII policy and recipient/access boundary | Deliverable contract | ELT Doc hydration and delivery ledger | The approved copy completed private-GCP writes and true no-ops without logging names. Fresh-source readback now certifies the exact Google-normalized table edge and preserved outside content while public evidence retains only hashed revision identity. Before recurring delivery or canonical use, retain an explicit recipient/access policy for the named E01 facts; public-safe operational logs are not authorization to broaden document access. |
| Fail-closed safety-store reachability evidence beyond the accepted paths | Delivery attempt | Each future recurring writer | **Tier 1, pipeline, Delivery RPS, RPS Tracking, and the first All Hires weekday shadow are closed; future writers remain gated independently.** All Hires run `945cd2a7-8108-411f-a3e2-6419d6542635` observed a fresh explicit `DISENGAGED` state inside its exact copy-only canary, completed one zero-mutation attempt, and was followed by an independently read-back `ENGAGED` state at `2026-07-17T15:19:58.890Z`. Weekly's fresh execution completed one durable zero-mutation attempt. All four pipeline rollovers, the Delivery dated rollover, and RPS's capacity/value canaries completed guarded writes with source freshness and stop-switch checks at permit and mutation boundaries. RPS's stable replay made zero mutations at Drive 39, and the stop was re-engaged after every window. An empty, delayed, or engaged state list must never be treated as reachability proof. |
| Typed value and fail-closed recovery proof | Governed workbook preimage × changed cell | Final Offer and future value writers | **Current-cycle gate closed.** The lossy RAW restore path was replaced with typed `userEnteredValue` preimages, changed-cell-only atomic requests, explicit empty `CellData` for true blanks, canonical blank/signed-zero fingerprints, advanced Drive-version settlement, and exact typed/structure postconditions. The shared copy has no exclusive human-editor lease, so ambiguous postconditions never trigger stale rollback. Final Offer completed a certified correction/no-op pair; All Hires completed a fresh write/no-op pair; Weekly Recruitment reached exact typed values and topology at Drive 48 and then completed a durable zero-mutation no-op. |
| Delayed Drive/Sheets post-state certification for large workbooks | Forward batch × settled typed postimage × Drive version | Weekly Recruitment and future large-sheet writers | **Implementation and live changed-write proof closed.** Commits `d9a4998`/`59c3acd` cover Weekly's observed lag and stop reads. Commit `dc8c6b5` applies the extended settlement only after a resolved forward structural mutation; preflight, ambiguous post-state, and rollback recovery retain the short bound. Job generation 11 uses 4 GiB / 3,072 MiB heap and 57 minutes. All four pipeline structural/value writes, Delivery, and RPS settled and certified exactly. RPS's early replays observed delayed Drive 36→37 and 38→39 publication and failed closed with zero mutations; the stable replay certified Drive 39→39. |
| Scheduled DOCX/HTML storage/runtime contract, if Python output is literal scope | Reporting week × generated artifact | ELT review workflow | Scope/runtime decision; current scheduled image is Node-only and service accounts cannot own Drive files. |
| Warehouse-equivalent scorecard identity, `submitted_at`, and historical application/job association | Scorecard × application-job association × submission event | RPS Tracking; Delivery Roles RPS | **Retained current-cycle continuity closed; downstream semantics remain open.** RPS Tracking preserves all 3,448 imported rows and appended the 153 accepted live identities from its complete 3,601-row canary source. Delivery preserves its imported Raw/Clean timestamp identities, validates them against the broader historical projection, and permits append only inside the report date's Friday–Thursday window; unseen out-of-window platform rows cannot backfill. The corrected Jul 13–16 daily reports replay exactly from the 3,618-scorecard source cut. Seven RPS requisition residuals are historical application transfers and remain preserved under their imported association instead of being rewritten to today's job. Keep the adapter boundary and retain the named `created_at` with `submitted_at` fallback clock; the shared clean/final/pivot time-only behavior is still a separate parity decision. |
| Deterministic materialization of `RPS Clean` and `RPS Final`, plus dependent formula/pivot reconciliation | Scorecard ledger row × clean/final identity × reporting grouping | RPS Tracking full workbook | **Open, fail-closed after live copy inventory.** Two stable read-only reads of all 23 tabs proved that `RPS Clean` and `RPS Final` contain zero formulas and zero pivots; they are literal materialized values, while three other tabs own pivots and eight downstream tabs own formulas. This contradicts the proposed Sheets-formula renderer. Define and approve the literal renderer, full-timestamp mapping, dependent recalculation order, and exact reconciliations before adding these surfaces to any writer. See `RPS_TRACKING_COPY_STRUCTURE_BASELINE_2026-07-17.md`. |
| Read-only canonical access for the ELT Doc and pipeline 907 | Canonical artifact × exact parity surface | Canonical-parity acceptance | True access/governance gap. The approved writer identity can edit every copy but cannot read these two canonical baselines. This blocks parity evidence and canonical cutover, not the completed copy-only operational hydrations. Provide Viewer access or a separately approved immutable baseline; do not mutate the canonicals under copy-only authorization. |
| Comparable current-cycle canonical surfaces for pipeline 890, pipeline 1026/1027, pipeline 1118/1119, and Final Offer | Canonical artifact × reporting-period tab/range | Canonical-parity acceptance | These canonicals are readable; this is not a permission failure. They lack the comparable `Candidate Level Data - 10 July` / Jul 10 candidate or `July Offer Data` surfaces needed for current-cycle parity. Retain the successful copy operational evidence separately and compare when an approved like-for-like baseline exists. |
| Existing Greenhouse OAuth credential exposure risk | Deployed hydrator revision | Every GCP source pull | Risk accepted by the operator for the copy-only staging demonstration on 2026-07-12; rotation is not an execution blocker. Continue using only the existing Secret Manager reference, never expose the value in source, arguments, logs, or evidence, and keep canonical writes impossible. |
| Cross-process Greenhouse OAuth token coordination | Source-pull execution lease | Existing hourly service plus staging hydrator jobs | **Closed for every staged artifact runner in this rollout.** One database claim serializes all staged source loads globally, an immutable payload is loaded once per run, and retries replay it without minting another token. The public production service remains out of scope and unchanged; its Scheduler was read-only verified paused. If that external path is resumed without joining the same claim, its owner must coordinate the schedule or adopt the shared source boundary before overlapping this copy Job. |

The deployed Job template remains `dry_run` with every write flag false. A
write-capable execution must opt into exact artifact flags, observe an explicit
durable `DISENGAGED` switch at each mutation boundary, and use an internally
minted copy/range/source-bound permit no longer than 15 minutes. The persistent
switch is re-engaged after guarded canaries; the legacy operator-managed
`ENABLED_AT`/`EXPIRES_AT` window is not an unattended Job dependency.
